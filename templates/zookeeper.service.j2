# {{ansible_managed}}

[Unit]
Description=ZooKeeper

[Service]
Type=simple
User={{ zk_user }}
Group={{ zk_group }}
ExecStart={{zk_dir}}/bin/zkServer.sh start-foreground

TimeoutSec=300

[Install]
WantedBy=multi-user.target
